- 👀 **See what your agents are doing.** While an agent works, the line under the dots tells you its current step and how long it has been at it: searching (with the query), reading a page, opening a skill, checking GitHub, waiting for your approval, thinking… **show steps** lists everything it has done in that task so far, so a long research job is no longer a black box.
- 💬 **Long replies open at their start.** When a reply is taller than the chat, the view now stops at the agent's avatar and the beginning of its text instead of jumping to the end, so you read it top to bottom.
- 📊 **Tighter agent cards.** The Statistics cards are shorter again: the name sits right under the avatar and the attributes take less room.
