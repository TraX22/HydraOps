- ⌨️ **Commands, phase 2.** Fifteen more verbs in the chat palette (and in Telegram): `/model`, `/engine` and `/aspect` to configure the active agent without leaving the chat; `/tools`, `/grant` and `/revoke` to edit its tools; `/crons`, `/cron mon-fri 09:00 …`, `/pause`, `/resume` and `/run` for scheduled tasks — `/cron` understands plain schedules in English and Spanish and opens the task form pre-filled; plus `/retry`, `/profile`, `/lang` and `/theme`.
- 📖 **Docs that match the app.** New README (banner, badges, everything shipped in September) and the manual's Telegram, scheduled-tasks, agents and add-ons pages brought in line with the commands, the schedule picker and delegation.
