- 🎬 **Three video engines.** Valentina can now render with Google Veo 3.1 (long-running operation, polled until the clip is ready), xAI Grok Imagine video, and Leonardo Motion. The engine selector only lists what the backend can actually drive, and the aspect selector only offers what the chosen engine supports (Veo: 16:9 / 9:16; Grok: all seven; Leonardo: 16:9, 9:16, 3:4).
- 🏷️ **The chat footer names the engine.** Generated images and videos show the LLM that wrote the prompt *and* the engine that rendered it, by its readable name ("Flux Dev", "Isometric Fantasy", "veo-3.1-fast-generate-preview") instead of an id.
- 🛠️ **Sturdier media workers.** A Leonardo model id can no longer be mistaken for a local LLM, and when a model narrates its `generate_image` / `generate_video` call instead of making it, the worker renders anyway with the prompt the model wrote.
- ⏱️ Video polling waits up to 8 minutes for both engines, and Leonardo requests use only sizes its 480p tier accepts.
