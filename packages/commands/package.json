{
  "name": "@hydraops/commands",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --out-dir dist",
    "dev": "tsup src/index.ts --format esm --out-dir dist --watch"
  },
  "devDependencies": {
    "tsup": "^8.5.0",
    "typescript": "^5.8.0"
  }
}
